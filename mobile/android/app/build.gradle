plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.algotrading.app'
    compileSdk 34

    defaultConfig {
        applicationId "com.algotrading.app"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    def backendBaseUrl = project.findProperty("backendBaseUrl") ?: "http://10.0.2.2:8000/"
    def backendWsUrl = project.findProperty("backendWsUrl") ?: "ws://10.0.2.2:8000"

    buildTypes {
        debug {
            buildConfigField 'String', 'BACKEND_BASE_URL', "\"${backendBaseUrl}\""
            buildConfigField 'String', 'BACKEND_WS_URL', "\"${backendWsUrl}\""
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            buildConfigField 'String', 'BACKEND_BASE_URL', "\"${backendBaseUrl}\""
            buildConfigField 'String', 'BACKEND_WS_URL', "\"${backendWsUrl}\""
        }
    }

    buildFeatures {
        buildConfig true
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }

    kotlinOptions { jvmTarget = '17' }
}

dependencies {
    implementation 'androidx.core:core-ktx:1.13.1'
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'androidx.lifecycle:lifecycle-runtime-ktx:2.8.4'
    implementation 'com.google.android.material:material:1.12.0'
    implementation 'com.squareup.okhttp3:okhttp:4.12.0'
    implementation 'com.squareup.retrofit2:retrofit:2.11.0'
    implementation 'com.squareup.retrofit2:converter-gson:2.11.0'
    implementation 'org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1'
}
